#include "stdafx.h"
#include <string>
#include <vector>
#include <regex>

class PersonalPronouns {
	std::vector<std::string> personal_pronouns;

public:
	PersonalPronouns(const std::string& text) {
	}
};